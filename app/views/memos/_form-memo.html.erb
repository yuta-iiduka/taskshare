<%= form_with model: memo,url: user_memos_path(current_user.id),local: true do |f| %>
  <%= f.text_area :memotext, row:'1',placeholder: "ToDo", class:"form-control input-sm" %><br>
  <%= f.submit "Add Memo", class: 'btn btn-secondary' %>
<% end %>
